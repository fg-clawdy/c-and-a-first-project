["Get these 4 values and add to your GitHub repository secrets (Settings \\u2192 Secrets and variables \\u2192 Actions):\\n\\n| Secret | Description | Value |\\n|--------|-------------|-------|\\n| `DATABASE_URL` | PostgreSQL connection | From Vercel Postgres or your provider |\\n| `NEXTAUTH_URL` | Your app URL | e.g., `https://your-app.vercel.app` |\\n| `NEXTAUTH_SECRET` | Secret for auth | Generate: `openssl rand -base64 32` |\\n| `NODE_ENV` | Environment | `production` |\\n\\n### Deploy\\n\\n1. Click **Deploy** in Vercel\\n2. Wait 1-2 minutes\\n3. Visit your Vercel URL\\n4. The app is now live, \"env", "build", "DATABASE_URL", "build", "NEXTAUTH_URL", "build", "NEXTAUTH_SECRET", "build", "NODE_ENV", "build", "npm install", "npx prisma generate", "npm run build", "Vercel"]